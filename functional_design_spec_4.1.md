# Functional Design Specification
**System Name:** Liberdat
**Section:** 4.1 Data Classification

This document maps the required Data Classification security standards directly to the current Liberdat codebase. It details what encryption is mandated, what is currently implemented, and what remains to be built to achieve full compliance.

## 4.1 Data Classification

| Classification | Description | Liberdat Code Examples & Exact Line Numbers | Target Encryption (Required by Specs) | Current Encryption (Implemented) & Code Lines | Remaining to Achieve Targeted Encryption |
| :--- | :--- | :--- | :--- | :--- | :--- |
| **Restricted** | Highest sensitivity; health & medical data requiring protection. | backend/prisma/schema.prisma<br/><br/>**Models:**<br/>- PatientDetails (Lines 302-311) → stores Date of Birth, Gender<br/>- DiseaseRecord (Lines 325-360) → stores disease type and patient link<br/>- DiagnosisRecord (Lines 447-463) → stores exact patient diagnosis answers and dates<br/>- TherapyRecord (Lines 494-506) → stores medications and treatments<br/>- SideEffectRecord (Lines 522-534) → stores side effects and intensity<br/>- EventRecord (Lines 582-592) → stores relapses, hospitalizations, pregnancies<br/>- MolecularAnalysisSelection (Lines 548-556) → stores molecular/genetic analysis results<br/>- HistologicalSubtypeSelection (Lines 570-578) → stores histological subtype data<br/>- HealthMetrics (Lines 644-655) → stores steps, heart rate, blood glucose, oxygen saturation | AES-256-GCM at rest + TLS 1.3 in transit + **Application-layer column-level encryption in DB** | **At-Rest (AWS RDS):** Implemented via AWS RDS infrastructure-level encryption. AWS encrypts the entire physical database storage volume using AES-256. No backend code lines applicable — this is an AWS configuration, not application code.<br/><br/>**In-Transit (TLS):** Implemented. All data traveling from the Node.js app to the AWS database is secured via SSL/TLS.<br/>**[Code Location:** backend/src/prisma/prisma.service.ts **Lines 14-16**]<br/>ssl: { rejectUnauthorized: false } | **[GAP — Missing Code]:** Application-layer column-level encryption not yet implemented.<br/><br/>Must build a Prisma Middleware inside backend/src/prisma/prisma.service.ts (around **Line 28**, inside onModuleInit()) that encrypts specific string fields using AES-256 before saving to the database, so data is unreadable even with direct DB access. |
| **Confidential** | Internal business data requiring protection. | backend/prisma/schema.prisma<br/><br/>**Models:**<br/>- User (Lines 76-108) → stores name, email, role<br/>- Team (Lines 240-254) → stores team name and organisation link<br/>- Session (Lines 133-152) → stores user session tokens and IP addresses | AES-256-GCM at rest + TLS 1.2+ in transit | **Fully Compliant**<br/><br/>**At-Rest:** Handled by AWS RDS default volume encryption. No backend code needed.<br/><br/>**In-Transit (TLS):** Implemented.<br/>**[Code Location:** backend/src/prisma/prisma.service.ts **Lines 14-16**]<br/>ssl: { rejectUnauthorized: false } | **None. Fully compliant.** |
| **Public** | Non-sensitive content shared with all authenticated users. | backend/src/scripts/<br/><br/>**Files:**<br/>- update-privacy-policy-en.ts (Entire File)<br/>- update-terms-of-use-en.ts (Entire File) | TLS 1.2+ in transit only | **Fully Compliant**<br/><br/>Data travels over standard HTTPS (TLS 1.2+). | **None. Fully compliant.** |
